# Runtime dependencies for the Heroku web app (installed by the buildpack).
# Test-only tooling lives in requirements-dev.txt so it stays out of the slug.
Flask>=2.3
gunicorn>=21.2
pymongo[srv]>=4.6
python-dotenv>=1.0
# Sanitises rich-text job descriptions. Ships prebuilt abi3 manylinux wheels,
# so Heroku installs it without a Rust toolchain.
nh3>=0.2.15
